<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".DirectorSortActivity$DummySectionFragment" >

    <ScrollView
        android:layout_width="fill_parent"
        android:layout_height="fill_parent" >

        <LinearLayout
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:orientation="vertical" >

            <ImageView
                android:id="@+id/imageView1"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
                android:layout_marginTop="0dp"
                android:contentDescription="@string/content_desc"
                android:scaleType="fitXY" />

            <ImageView
                android:id="@+id/defaultMapTile1"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:adjustViewBounds="true"
                android:contentDescription="@string/filmlist_icon_desc"
                android:scaleType="centerCrop"
                android:src="@drawable/map_unavailable"
                android:visibility="gone" >
            </ImageView>

            <!-- film panel list view image tiles -->

            <ListView
                android:id="@+id/listview"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
                android:clickable="true" />
            <!-- film panel list view image tiles -->

            <ListView
                android:id="@+id/listviewMapTiles"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
                android:cacheColorHint="@android:color/transparent"
                android:listSelector="@android:color/transparent" />

            <TextView
                android:id="@+id/film_title"
                style="@style/CodeFont"
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="10dp"
                android:layout_marginLeft="10dp"
                android:layout_marginTop="0dp"
                android:gravity="bottom|left"
                android:textColor="@color/white_overlay"
                android:textSize="90sp"
                android:textStyle="bold" />
        </LinearLayout>
    </ScrollView>

</RelativeLayout>